import{_ as n,M as c,p as t,q as d,R as s,t as a,N as o,a1 as l}from"./framework-9d7a10f9.js";const i={},p=s("h1",{id:"css笔记",tabindex:"-1"},[s("a",{class:"header-anchor",href:"#css笔记","aria-hidden":"true"},"#"),a(" CSS笔记")],-1),r={href:"https://www.cnblogs.com/wangfupeng1988/p/4325007.html",target:"_blank",rel:"noopener noreferrer"},h=l(`<p>CSS——Cascading Style Sheets——层叠样式表。从字面意思来看，层叠的意思是需要多层叠加起来。在这些层次中有一个“浏览器默认样式”层，即浏览器默认的各个html元素的样式。</p><p>webkit内核渲染html和css，html的解析是一条线，css的解析是一条线，两者会在某一点结合，形成最终的网页。浏览器将载入的html变为dom树，但是此时没有任何显示样式。所以显示的样式，都是css定义的，浏览器只会通过css来渲染视图样式。</p><p>这会联想到一些关于CSS的问题：浏览器是如何解析CSS，CSS和html如何结合起来——选择器，布局，CSS能控制哪些显示方式。</p><h2 id="浏览器是如何解析css" tabindex="-1"><a class="header-anchor" href="#浏览器是如何解析css" aria-hidden="true">#</a> 浏览器是如何解析CSS</h2><p>CSS的样式来源有5个，不过程序真正控制的只有3个，还有就是浏览器的</p><ol><li><code>&lt;div style=&#39;&#39;&gt;&lt;/div&gt;</code> 属性样式（行内样式）</li><li><code>&lt;style&gt;</code> 内部样式（style标签）</li><li><code>&lt;link&gt;</code> 外部样式（引入的样式文件）</li><li>浏览器用户自定义样式</li><li>浏览器默认样式</li></ol><h2 id="css和html如何结合起来——选择器" tabindex="-1"><a class="header-anchor" href="#css和html如何结合起来——选择器" aria-hidden="true">#</a> CSS和html如何结合起来——选择器</h2><p>CSS如何与html结合起来是通过选择器。CSS提供了多种多样的选择器类型，而且每个级别都在不断的增加新的选择器类型，使得选择器更加灵活易用。对css选择器来说，有一个很重要的话题——css选择器级别的判断问题。这涉及到一个概念 ———— 特指度。</p><h2 id="页面呈现" tabindex="-1"><a class="header-anchor" href="#页面呈现" aria-hidden="true">#</a> 页面呈现</h2><p>页面呈现可以分为两类——文字，块。</p><ol><li>针对文字来说，我们可以设置字体、字号、加粗、斜体、背景色等等；</li><li>对于块来说情况比较多，有盒子模型、浮动、定位、display、背景等；</li></ol><h1 id="浏览器默认样式" tabindex="-1"><a class="header-anchor" href="#浏览器默认样式" aria-hidden="true">#</a> 浏览器默认样式</h1><h2 id="display" tabindex="-1"><a class="header-anchor" href="#display" aria-hidden="true">#</a> display</h2><p>常见的display属性设置有：<code>display: block</code>,<code>display: list-item</code>,<code>display: table</code>,<code>display: table-cell</code>,<code>display: inline-block</code>等</p><h2 id="block元素" tabindex="-1"><a class="header-anchor" href="#block元素" aria-hidden="true">#</a> block元素</h2><p>浏览器默认的block元素主要有：<code>html,body,div,address,blockquote,h1,h2,h3,h4,h5,h6,ol,ul,dl,dt,dd,form</code>等。</p><p>没有设置<code>block</code>的元素，默认为 <code>inline</code> 显示</p><h2 id="display-list-item" tabindex="-1"><a class="header-anchor" href="#display-list-item" aria-hidden="true">#</a> display: list-item</h2><p>设置这个样式之后，就和浏览器默认的 ul-li 元素的样式一样了</p><h2 id="dispaly-table" tabindex="-1"><a class="header-anchor" href="#dispaly-table" aria-hidden="true">#</a> dispaly: table</h2><p><code>div</code>和<code>table</code>元素最大的区别在于，div的宽度和父元素一样，table宽度根据内容而定，即table具有“包裹性” <code>dispaly: block;</code> 和 <code>display: table;</code></p><h2 id="display-table-cell" tabindex="-1"><a class="header-anchor" href="#display-table-cell" aria-hidden="true">#</a> display: table-cell</h2><p>列表中的元素 <code>td,th</code> 等默认样式是用的 <code>display:table-cell</code>,可以用它干一件很重要的事情 ———— 多列布局</p><h2 id="display-inline-block" tabindex="-1"><a class="header-anchor" href="#display-inline-block" aria-hidden="true">#</a> display: inline-block</h2><p><code>button,input,textarea,object,select</code>等元素默认是<code>display: inline-block;</code>, 行内块级元素</p><h2 id="body元素" tabindex="-1"><a class="header-anchor" href="#body元素" aria-hidden="true">#</a> body元素</h2><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">body</span> <span class="token punctuation">{</span><span class="token property">display</span><span class="token punctuation">:</span> block<span class="token punctuation">;</span><span class="token property">margin</span><span class="token punctuation">:</span> 8px<span class="token punctuation">;</span><span class="token punctuation">}</span>
</code></pre></div><p>在body中默认定义了两个样式。不同浏览器为body设置的margin值可能不一样，在 css 中用 <code>* {margin: 0;padding: 0;}</code> 解决浏览器样式兼容性问题。</p><p>浏览器默认样式还为body设置了line-height，line-height: 1.12是一个相对值，即是文字高度的1.12倍</p><h2 id="br-换行的实现" tabindex="-1"><a class="header-anchor" href="#br-换行的实现" aria-hidden="true">#</a> <code>&lt;br&gt;</code>换行的实现</h2><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token selector">br::before</span> <span class="token punctuation">{</span>
  <span class="token property">content</span><span class="token punctuation">:</span> <span class="token string">&#39;\\A&#39;</span><span class="token punctuation">;</span>
  <span class="token property">white-space</span><span class="token punctuation">:</span> pre-line<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
// 换行是\`<span class="token property">white-space</span><span class="token punctuation">:</span> pre-line\`效果
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><h1 id="选择器" tabindex="-1"><a class="header-anchor" href="#选择器" aria-hidden="true">#</a> 选择器</h1><p>css中定义的样式，要将这些样式设置到html节点上，就需要通过选择器了。让浏览器知道css选择了哪一个dom节点。 一个浏览器使用css流程，和学习css的思路：css样式的加载和层叠 -&gt; 择器以及选择器的等级 -&gt; 呈现的各种样式（背景、字体、定位、浮动等）</p><p>选择器有很多，不过大可分为两类：</p><ol><li>标签选择器（*是特殊情况），可但标签，也可上下文多标签；</li><li>属性选择器（id和class都是属性，特殊的属性）；</li></ol><h2 id="标签选择器" tabindex="-1"><a class="header-anchor" href="#标签选择器" aria-hidden="true">#</a> 标签选择器</h2><h3 id="通用选择器" tabindex="-1"><a class="header-anchor" href="#通用选择器" aria-hidden="true">#</a> 通用选择器 *</h3><p>通用选择器 <code>*</code> 最常用的就是<code>* {margin: 0;padding: 0;}</code></p><h3 id="单标签" tabindex="-1"><a class="header-anchor" href="#单标签" aria-hidden="true">#</a> 单标签</h3><p>单标签选择器很基础了，到处都在用</p><h3 id="多标签" tabindex="-1"><a class="header-anchor" href="#多标签" aria-hidden="true">#</a> 多标签</h3><p>多标签选择器一般和html上下文有关，有以下几种分类</p><ol><li>选择一个指定祖先元素的所有子元素，例如：<code>div p {}</code></li><li>选择一个父元素的所有直属子元素，例如：<code>div &gt; p {}</code></li><li>选择某一个元素紧挨着的兄弟元素，例如： <code>li + li {}</code></li><li>选择某一个元素的所有同胞元素，例如： <code>span ~ a {}</code></li></ol><p>以上的各种选择器可各自组合，不过不要过于复杂，要保证代码的可读性</p><p>例如使用 <code>ul + li</code> 要在 各个菜单之间加下划线。之前的实现思路是每个<code>li</code>都加上<code>border-bottom</code>,然后用伪类选择器选择最后一个将<code>boder</code>去掉。 实际上没必要这么麻烦，下面是另一种思路：</p><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">ul li+li</span> <span class="token punctuation">{</span>
  <span class="token property">border-top</span><span class="token punctuation">:</span> 1px solid #ccc<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>挺有意思</p><h2 id="属性选择器" tabindex="-1"><a class="header-anchor" href="#属性选择器" aria-hidden="true">#</a> 属性选择器</h2><h3 id="特殊-id选择器-class选择器" tabindex="-1"><a class="header-anchor" href="#特殊-id选择器-class选择器" aria-hidden="true">#</a> 特殊： id选择器，class选择器</h3><p>css选择器是根据 html 节点的特性来设定的，id选择器和class选择器属于节点的属性，只是它们比较特殊，每个html节点的id不能重复。</p><p>由于比较特殊和常用，所以单独给 <code>id</code> 和 <code>class</code> 单独的选择器标识，本质上它们还是属于属性选择器。</p><p>例如，选择id为div1的元素</p><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">#div1</span> <span class="token punctuation">{</span><span class="token property">color</span><span class="token punctuation">:</span> red<span class="token punctuation">;</span><span class="token punctuation">}</span>
<span class="token selector">div[id=&#39;#div1&#39;]</span> <span class="token punctuation">{</span><span class="token property">color</span><span class="token punctuation">:</span> red<span class="token punctuation">;</span><span class="token punctuation">}</span>
</code></pre></div><h3 id="属性选择器-1" tabindex="-1"><a class="header-anchor" href="#属性选择器-1" aria-hidden="true">#</a> 属性选择器</h3><p>属性选择器有两种情况：</p><ol><li>只通过属性名选择，<code>img[title] {}</code></li><li>通过属性名和属性值选择，<code>input[type=&#39;text&#39;] {}</code></li></ol><h3 id="伪类和伪元素" tabindex="-1"><a class="header-anchor" href="#伪类和伪元素" aria-hidden="true">#</a> 伪类和伪元素</h3><p>伪类和伪元素可针对任何一种选择器使用</p><p><strong>伪类</strong><br> 伪类分为 UI伪类 和 结构化伪类。</p><ol><li>UI伪类 UI伪类比较简单常用，</li></ol><div class="language-css line-numbers-mode" data-ext="css"><pre class="language-css"><code><span class="token comment">/* 适用于未被访问的链接；*/</span>
<span class="token selector">a:link</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>

<span class="token comment">/* 适用于已经访问过的链接； */</span>
<span class="token selector">a:visited</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>

<span class="token comment">/* 鼠标指针指向一个元素，还未激活； */</span>
<span class="token selector">a:hover</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>

<span class="token comment">/* 适用于一个元素被选择但未被释放； */</span>
<span class="token selector">a:active</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>四个伪类的声明顺序应该是： <code>:link, :visited, :hover, active</code>, <code>a:hover</code> 必须放到 <code>a:link</code>,<code>a:visited</code> 之后，否则将隐藏 <code>a:hover</code> 内定义的相关规则。<code>a:active</code> 应该放在 <code>a:hover</code> 之后，否则<code>a:active</code>内的样式将隐藏。</p><ol start="2"><li>结构化伪类（低版本IE不行） 常见的应用比如 实现一个表格间隔显示背景颜色，最简单的方式是使用结构化伪类</li></ol><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">table tr:nth-child(even)</span> <span class="token punctuation">{</span>
  <span class="token property">background-color</span><span class="token punctuation">:</span> #f1f1f1<span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p>还有一些其他的常用书写：</p><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">table tr:nth-child(event)</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
<span class="token selector">table tr:nth-child(odd)</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
<span class="token selector">table tr:first-child</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
<span class="token selector">table tr:last-child</span> <span class="token punctuation">{</span><span class="token punctuation">}</span>
</code></pre></div><p><code>css3</code> 新增了很多伪类选择器</p><p><strong>伪元素</strong> 在<code>css3</code>的规范中，单冒号(<code>:</code>)用于CSS3伪类，双冒号(<code>::</code>)用于CSS3伪元素</p><p>伪元素中 <code>::before</code>,<code>::after</code> 非常常用,</p><div class="language-css" data-ext="css"><pre class="language-css"><code><span class="token selector">div::before</span> <span class="token punctuation">{</span>
  <span class="token property">content</span><span class="token punctuation">:</span> <span class="token string">&#39;CSS3&#39;</span><span class="token punctuation">;</span>
<span class="token punctuation">}</span>
</code></pre></div><p><code>content</code>属性可以设置内容，内容也可以写成 <code>Unicode</code> 编码</p><h2 id="选择器扩展" tabindex="-1"><a class="header-anchor" href="#选择器扩展" aria-hidden="true">#</a> 选择器扩展</h2><p>选择器本来是css的一种规则，用于为css选择html节点的。但是也有人利用这些特性创造出优秀的作品。</p><ul><li><p>jQuery被推广流行的根本原因就是它的“Query”——基于css选择器的“Query”。jquery可以通过一段css选择器表达式从既有的html结构中选择符合表达式的dom组。</p></li><li><p>zen-Coding，在编辑器中常用的html代码生成插件，可以根据css选择器表达式创造出html节点。</p></li></ul><div class="language-txt" data-ext="txt"><pre class="language-txt"><code>div#div1&gt;ul&gt;li*3
</code></pre></div><h1 id="选择器的优先级" tabindex="-1"><a class="header-anchor" href="#选择器的优先级" aria-hidden="true">#</a> 选择器的优先级</h1><p><code>!important</code> &gt; 行内样式 &gt; id选择器 &gt; class选择器 &gt; tag &gt; * &gt; 继承 &gt; 默认</p><p>选择器从右往左解析</p><h2 id="特指度" tabindex="-1"><a class="header-anchor" href="#特指度" aria-hidden="true">#</a> 特指度</h2><p>特指度（specificity）。特指度表示一个css选择器表达式的重要程度，可以通过 <code>I-C-E</code> 计算公式，来计算出一个数值，数越大，越重要。</p><ul><li><code>I——Id</code></li><li><code>C——Class</code></li><li><code>E——Element</code></li></ul><p>针对一个css选择器表达式，遇到一个id就往特指度数值中加100，遇到一个class就往特指度数值中加10，遇到一个element就往特指度数值中加1。</p><div class="custom-container warning"><p class="custom-container-title">提示</p><p>!important优先级最高，高于上面一切。* 选择器最低，低于一切。</p></div><h1 id="盒子模型" tabindex="-1"><a class="header-anchor" href="#盒子模型" aria-hidden="true">#</a> 盒子模型</h1><p>页面渲染时，DOM元素所采用的布局模型。包含内容区、内边距、边框、外边距。</p>`,85);function u(k,b){const e=c("ExternalLinkIcon");return t(),d("div",null,[p,s("p",null,[a("当初学习CSS时找到了"),s("a",r,[a("王福朋 CSS知多少 系列博客"),o(e)]),a("，受益良多，讲的很通俗易懂，解释了心中的很多疑惑，这里把一些知识点和概念记下来增强印象，有些文字直接使用原文中的，因为觉得原文的描述已经很好了。")]),h])}const m=n(i,[["render",u],["__file","CSS笔记.html.vue"]]);export{m as default};
